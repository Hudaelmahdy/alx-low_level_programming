#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *@str: string.
 * Return: Always 0.
 */
void print_rev(char *str)
{
		int i = 0;

		while (s[i])
			i++;
		while (i--)
		{
			_putchar(s[i]);
		}
		_putchar('\n');
}
