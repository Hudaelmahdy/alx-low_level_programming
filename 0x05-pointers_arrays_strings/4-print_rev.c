#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *@str: string.
 * Return: Always 0.
 */
void print_rev(char *str);
int main(void)
{
		char *str;

		str = "Dolly";
		print_rev(str);
		return (0);
}
